android {
  compileSdkVersion 35

  defaultConfig {
    minSdkVersion 26
    targetSdkVersion 35
  }

  compileOptions {
    setSourceCompatibility(Versions.javaVersion)
    setTargetCompatibility(Versions.javaVersion)
  }

  testOptions {
    animationsDisabled = true
    unitTests {
      includeAndroidResources = true
      returnDefaultValues = true
    }
  }

  kotlinOptions {
    jvmTarget = Versions.javaVersion.toString()
  }
}
